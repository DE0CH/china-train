# Vercel function env (Project Settings → Environment Variables), not needed by the browser:
# PROXY_URL=http://user:pass@host:port   # IPRoyal proxy the function uses to reach 12306 (optional; direct if unset)
