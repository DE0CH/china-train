# Train API (jisutrain.market.alicloudapi.com)
# API key can be set via the app login page (stored in cookies).
# Optional: set API_KEY here for server-side use only (e.g. scripts).
# Get your APPCODE from Alibaba Cloud API Marketplace.
# API_KEY=your_appcode_here
